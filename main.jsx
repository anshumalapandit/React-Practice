import React from "react"
import ReactDOM from 'react-dom/client';

const MyFirstLine=<h1>This is my first line i am so happy</h1> // this is not a html markup its is jsx which looks like html
//now display this first line ur
ReactDOM.createRoot(document.getElementById("root")).render(MyFirstLine);
